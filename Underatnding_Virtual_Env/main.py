import requests
import pandas 
import logging 
logging.basicConfig(level=logging.INFO, format='%(levelname)s - %(message)s - %(asctime)s')
logging.info("Starting the data processing pipeline.")
logging.info("Fetching data from the API.")
logging.info("Data fetched successfully.")
logging.info("Processing the data.")
logging.info("Data processed successfully.")
logging.info("Saving the data to a file.")
logging.error("Data saved successfully.")    
